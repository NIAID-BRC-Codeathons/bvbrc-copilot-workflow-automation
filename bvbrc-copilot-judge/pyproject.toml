[project]
name = "bvbrc-copilot-judge"
version = "0.1.0"
description = "Stage-1 AI-judge pipeline: query BV-BRC Copilot and Claude independently for step->BV-BRC-service JSON, score both against mined ground truth, and report 3-way overlap."
requires-python = ">=3.13"
dependencies = [
    "requests>=2.34.2",
    "python-dotenv>=1.0.1",
]

[tool.uv]
package = false
